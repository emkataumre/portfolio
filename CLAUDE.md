# Portfolio

Domain glossary: `CONTEXT.md`. Use its terms.

## Agent skills

### Issue tracker

GitHub issues on `emkataumre/portfolio`. The wayfinder map is issue #1. See `docs/agents/issue-tracker.md`.

## Git workflow

No pull requests. Work on a feature branch, then merge it into `main` locally and push `main`. Every push to `main` deploys to Cloudflare Pages.
